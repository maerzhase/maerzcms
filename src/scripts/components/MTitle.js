'use strict';

var React = require('react/addons');

require('styles/MTitle.scss');

var MTitle = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for MTitle</p>
        </div>
      );
  }
});

module.exports = MTitle; 

